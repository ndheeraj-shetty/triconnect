'use client';

import { useState } from 'react';
import { useRouter } from 'next/navigation';
import { motion } from 'framer-motion';
import type { LucideIcon } from 'lucide-react';
import { AlertCircle, ArrowRight, Building2, GraduationCap, HeartHandshake, KeyRound, Layers, Loader2, Lock, Mail, School, ShieldCheck, Sparkles, UserRoundCheck } from 'lucide-react';
import { useAuth } from '@/context/AuthContext';

const infoCards = [
  { title: 'Student Security First', text: 'For student privacy and security, self-registration has been disabled. Only students officially registered by the school can access the Student Portal.', icon: ShieldCheck },
  { title: 'Admin Registration Required', text: 'The School Administrator creates the student name, ID, email address, class, section, and temporary password in the Admin Portal.', icon: UserRoundCheck },
  { title: 'Student Login', text: 'Once registration is complete, students sign in with their registered email address or Student ID and password.', icon: KeyRound },
  { title: 'Hackathon Demonstration', text: 'Only Student Portal authentication is enabled for the demo. Admin, Teacher, and Parent portals open directly so judges can explore every feature.', icon: Sparkles },
];
const demoPortals: Array<{ label: string; href: string; icon: LucideIcon }> = [
  { label: 'Admin Portal', href: '/dashboard/admin', icon: Building2 },
  { label: 'Teacher Portal', href: '/dashboard/teacher', icon: School },
  { label: 'Parent Portal', href: '/dashboard/parent', icon: HeartHandshake },
];

export default function StudentLoginPage() {
  const router = useRouter(); const { login } = useAuth();
  const [username, setUsername] = useState(''); const [password, setPassword] = useState(''); const [remember, setRemember] = useState(true); const [loading, setLoading] = useState(false); const [error, setError] = useState('');
  const missingAccount = error.startsWith('Account Not Found');
  const submit = async (event: React.FormEvent) => { event.preventDefault(); setLoading(true); setError(''); try { const result = await login(username, password); if (result.role !== 'student') throw new Error('This login is reserved for student accounts.'); if (!remember) sessionStorage.setItem('triconnect_session_only', 'true'); router.push('/dashboard/student'); } catch (cause) { setError(cause instanceof Error ? cause.message : 'Incorrect password. Please try again.'); setLoading(false); } };
  return <main className="min-h-screen bg-white text-slate-900"><div className="grid min-h-screen grid-cols-1 lg:grid-cols-12">
    <aside className="relative hidden overflow-hidden border-r border-slate-200 bg-[#F8FAFC] p-10 lg:col-span-7 lg:flex lg:flex-col xl:p-12"><div className="absolute -left-20 -top-20 h-96 w-96 rounded-full bg-indigo-200/25 blur-3xl" /><div className="absolute -bottom-32 right-0 h-96 w-96 rounded-full bg-sky-200/30 blur-3xl" /><div className="relative flex items-center gap-2.5"><span className="grid h-9 w-9 place-items-center rounded-lg bg-gradient-to-tr from-blue-600 to-indigo-600 text-white shadow-sm"><Layers className="h-4.5 w-4.5" /></span><span className="text-lg font-bold tracking-tight">triconnect</span></div><div className="relative my-auto max-w-2xl"><motion.div initial={{ opacity: 0, y: 12 }} animate={{ opacity: 1, y: 0 }} className="inline-flex items-center gap-2 rounded-full border border-blue-200 bg-blue-50 px-3 py-1 text-[10px] font-bold uppercase tracking-wider text-blue-700"><Sparkles className="h-3.5 w-3.5" />Hackathon Demonstration</motion.div><h1 className="mt-5 text-4xl font-extrabold tracking-tight text-slate-900">triconnect Authentication</h1><p className="mt-3 max-w-xl text-sm leading-6 text-slate-500">A safe, school-managed access flow designed to protect student accounts while making the demo effortless to explore.</p><div className="mt-7 grid gap-3 sm:grid-cols-2">{infoCards.map((item, index) => { const Icon = item.icon; return <motion.article key={item.title} initial={{ opacity: 0, y: 12 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: .08 + index * .07 }} className="rounded-2xl border border-slate-200 bg-white p-4 shadow-sm"><span className="grid h-8 w-8 place-items-center rounded-lg bg-indigo-50 text-indigo-600"><Icon className="h-4 w-4" /></span><h2 className="mt-3 text-xs font-bold text-slate-800">{item.title}</h2><p className="mt-1.5 text-[11px] leading-5 text-slate-500">{item.text}</p></motion.article>})}</div></div><p className="relative text-[10px] text-slate-400">© {new Date().getFullYear()} triconnect · Secure school intelligence</p></aside>
    <section className="flex flex-col justify-center px-5 py-10 sm:px-10 lg:col-span-5 lg:px-12"><div className="mx-auto w-full max-w-md"><button onClick={() => router.push('/')} className="mb-6 flex items-center gap-2 text-xs font-semibold text-slate-500 hover:text-slate-900 lg:hidden"><ArrowRight className="h-3.5 w-3.5 rotate-180" />Back to Home</button><div className="rounded-3xl border border-slate-200 bg-white p-7 shadow-xl shadow-slate-100 sm:p-8"><div className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-xl bg-gradient-to-br from-blue-600 to-indigo-600 text-white"><GraduationCap className="h-5 w-5" /></span><div><p className="text-[10px] font-bold uppercase tracking-[.16em] text-blue-600">Student Portal</p><h2 className="text-xl font-semibold text-slate-900">Welcome back</h2></div></div><p className="mt-5 text-sm leading-6 text-slate-500">Use your student email or ID to securely access your learning dashboard.</p>{missingAccount ? <div className="mt-5 rounded-2xl border border-amber-200 bg-amber-50 p-4"><h3 className="text-sm font-bold text-amber-900">Account Not Registered</h3><p className="mt-2 text-xs leading-5 text-amber-800">Your email address or Student ID has not yet been registered by your School Administrator. Please contact your School Administrator to create your student account before attempting to sign in.</p><div className="mt-4 flex gap-2"><a href="mailto:office@westsideacademy.edu" className="rounded-lg bg-amber-600 px-3 py-2 text-[11px] font-bold text-white">Contact Administrator</a><button onClick={() => router.push('/')} className="rounded-lg border border-amber-200 bg-white px-3 py-2 text-[11px] font-bold text-amber-800">Back to Home</button></div></div> : error && <div className="mt-5 flex gap-2 rounded-xl border border-rose-200 bg-rose-50 p-3 text-xs text-rose-700"><AlertCircle className="h-4 w-4 shrink-0" />{error}</div>}<form onSubmit={submit} className="mt-6 space-y-4"><label className="block text-xs font-semibold text-slate-700">Student email or ID<div className="relative mt-2"><Mail className="absolute left-3 top-3 h-4 w-4 text-slate-400" /><input value={username} onChange={e => setUsername(e.target.value)} placeholder="Enter your registered Email or Student ID" required className="w-full rounded-xl border border-slate-200 py-2.5 pl-10 pr-3 text-sm outline-none focus:border-blue-500" /></div></label><label className="block text-xs font-semibold text-slate-700">Password<div className="relative mt-2"><Lock className="absolute left-3 top-3 h-4 w-4 text-slate-400" /><input type="password" value={password} onChange={e => setPassword(e.target.value)} placeholder="Enter your password" required className="w-full rounded-xl border border-slate-200 py-2.5 pl-10 pr-3 text-sm outline-none focus:border-blue-500" /></div></label><div className="flex items-center justify-between"><label className="flex items-center gap-2 text-xs text-slate-600"><input type="checkbox" checked={remember} onChange={e => setRemember(e.target.checked)} className="accent-blue-600" />Remember me</label><button type="button" className="text-xs font-semibold text-blue-600 hover:underline">Forgot password?</button></div><button disabled={loading} className="flex w-full items-center justify-center gap-2 rounded-xl bg-blue-600 py-3 text-xs font-bold text-white hover:bg-blue-700 disabled:opacity-60">{loading ? <Loader2 className="h-4 w-4 animate-spin" /> : <>Sign in <ArrowRight className="h-4 w-4" /></>}</button></form><div className="mt-5 rounded-xl bg-slate-50 p-3 text-[11px] leading-5 text-slate-500"><ShieldCheck className="mr-1 inline h-3.5 w-3.5 text-emerald-600" />Student sessions use secure JWT authentication. Self-registration is not available.</div></div><div className="mt-5 grid grid-cols-3 gap-2">{demoPortals.map(({ label, href, icon: PortalIcon }) => <button key={label} onClick={() => router.push(href)} className="rounded-xl border border-slate-200 bg-white p-3 text-center shadow-sm transition hover:-translate-y-0.5 hover:border-indigo-200"><PortalIcon className="mx-auto h-4 w-4 text-indigo-600" /><span className="mt-2 block text-[10px] font-bold text-slate-700">{label}</span><span className="mt-1 block text-[9px] text-slate-400">Open demo</span></button>)}</div></div></section>
  </div></main>;
}
